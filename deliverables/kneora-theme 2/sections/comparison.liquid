{% if section.settings.enabled %}<section class="wrap section-space"><div class="section-heading"><h2>{{ section.settings.heading | escape }}</h2></div><div class="table-scroll"><table><caption class="visually-hidden">{{ section.settings.heading | escape }}</caption><thead><tr><th scope="col">{{ section.settings.column_feature | escape }}</th><th scope="col">{{ 'product.single' | t }}</th><th scope="col">{{ 'product.bundle_label' | t }}</th></tr></thead><tbody>{% for block in section.blocks %}<tr {{ block.shopify_attributes }}><th scope="row">{{ block.settings.feature | escape }}</th><td>{{ block.settings.single | escape }}</td><td>{{ block.settings.bundle | escape }}</td></tr>{% endfor %}</tbody></table></div></section>{% endif %}
{% schema %}
{
  "name": "Package comparison",
  "settings": [
    {
      "type": "checkbox",
      "id": "enabled",
      "label": "Show comparison",
      "default": false
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Choose what belongs in your routine."
    },
    {
      "type": "text",
      "id": "column_feature",
      "label": "First column heading",
      "default": "Included"
    }
  ],
  "blocks": [
    {
      "type": "row",
      "name": "Comparison row",
      "settings": [
        {
          "type": "text",
          "id": "feature",
          "label": "Feature",
          "default": "Dual-knee massager"
        },
        {
          "type": "text",
          "id": "single",
          "label": "Single package",
          "default": "One"
        },
        {
          "type": "text",
          "id": "bundle",
          "label": "Bundle package",
          "default": "One"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Package comparison"
    }
  ]
}
{% endschema %}
